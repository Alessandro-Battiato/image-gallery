import React from "react";
import cat from "../../cat.PNG";

const Card = () => {
  return (
    <div>
      <figure>
        <img src={cat} />
      </figure>
    </div>
  );
};

export default Card;
